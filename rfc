#!/bin/bash
echo "本脚本可以批量替换文件中的字符串。如：多个文件中包含1234可批量替换为5678这样的形式。";
echo "-----------------------------------------------------------------------"
echo "输入路径";
read PWD;
echo "要替换的字符串"
read volold;
echo "替换成的字符串"
read volnew;
sed -i "s/$volold/$volnew/g" `grep $volold -rl $PWD `
echo "目录 $PWD 中所有的 $volold 都替换成了 $volnew!"
echo "完成咯，去看看吧！>>>>>";
echo "---------------------------------------------------------------------"
